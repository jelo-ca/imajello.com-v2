GEMINI_API_KEY=AIza...
PORT=3000

# Optional. Directory for leaderboard.json. Default: server/data (inside the repo).
# On Hostinger, point this outside the deploy folder so redeploys don't wipe scores:
# LEADERBOARD_DATA_DIR=/var/lib/imajello
# LEADERBOARD_DATA_DIR=/home/YOUR_USER/imajello-data
