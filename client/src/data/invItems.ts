import { content } from '../content';
export type { InvPhoto, InvItem } from '../content';

export const INV_ITEMS = content.invItems.items;
export const INV_GRID_SIZE = content.invItems.gridSize;
export const INV_FILLED_POSITIONS = content.invItems.filledPositions;
